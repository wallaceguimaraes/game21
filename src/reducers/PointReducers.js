const initialState = {
    player: 0,
    computer: 0
}

/**
 * Retorna uma state alterada ou nao
 */
const PointReducers = ( state = [], action ) => {
    if(state.length === 0){
        return initialState;
    }


    return state;

}

export default Reducers;